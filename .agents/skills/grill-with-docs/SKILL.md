---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, synchronizing the repository's existing decision and domain records as needed.
disable-model-invocation: true
---

Call the Skill tool twice, for "grilling" and "domain-modeling". When decisions become durable,
use the repository's existing `docs/decisions/` and accepted domain/intent records; do not create
parallel `docs/adr/` or glossary systems. Follow `docs/agents/skill-governance.md` for mappings.
